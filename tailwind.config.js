/** @type {import('tailwindcss').Config} */
module.exports = {
  // Specify the content to be scanned for classes
  content: ['./src/**/*.{js,ts,jsx,tsx,mdx}'],

  // Define the theme configuration
  theme: {
    extend: {}, // You can extend the default theme here
  },

  // No custom plugins included at the moment
  plugins: [],
};
